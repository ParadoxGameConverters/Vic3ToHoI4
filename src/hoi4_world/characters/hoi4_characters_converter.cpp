#include "src/hoi4_world/characters/hoi4_characters_converter.h"

std::map<int, hoi4::Character> hoi4::ConvertCharacters(const std::map<int, vic3::Character>& source_characters,
    const std::map<int, vic3::InterestGroup>& igs)
{
   return std::map<int, Character>();
}
